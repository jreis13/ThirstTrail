
<div class="container">
  <h2 class="team-title">My cocktails</h2>
  <hr class="divider">
  <div class="questions-container">
    <%# <p class="instructions">Your cocktails:</p> %>
    <div class="row">
      <% @favorites.each do |favorite| %>
        <div class="col-12 col-md-6">
            <div class="card card-recipe-fav mx-auto" id="<%= favorite.favoritable.id %>" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.4)), url(<%=favorite.favoritable.image_url%>); background-size: cover" >
            <%= render "recipes/favorite", recipe: favorite.favoritable %>
            <%= link_to recipe_path(favorite.favoritable), class: "bigger-link" do %>
              <h2 class="card-recipe-title"><%= favorite.favoritable.name %></h2>
            <% end %>
            </div>
        </div>
      <% end %>
      <hr>
      <hr>
    </div>
      <div class="btns-start">
        <%= link_to "Mix again!", new_preference_path, class: "btn btn-ghost-build" %>
        <p class="info-details-luck-fav">Feeling lucky? 🍀</p>
        <p class="info-details-start-fav">Try our <strong><%= link_to "cocktail roulette", roulette_path, class: "roulette-link" %></strong>.</p>
      </div>
</div>




  <%# <% @favorites.each do |selection| %>
    <%# <h1><%= selection.favoritable.name</h1> %>
  <%# <% end %>
