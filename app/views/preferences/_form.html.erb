<%= simple_form_for @preference do |f| %>
    <div class="questions-container">
      <%= f.input :alcohol, as: :radio_buttons, collection: @alcohol, input_html: { class: 'remove-button'}, wrapper_html: { class: 'questions-cards' }, label: 'Would you like alcohol on your cocktail?' %>
      <p class='hint'>💡 Did you know? A non-alcoholic cocktail is called a mocktail or a virgin cocktail.</p>
    </div>
    <div class="questions-container">
      <%= f.input :event_type, as: :radio_buttons, collection: @event_type, input_html: { class: 'remove-button'}, wrapper_html: { class: 'questions-cards' }, label: 'What type of event are you organizing?', hint: '', label_html: { class: 'selectable-card' }, input_html: { class: 'remove-button'} %>
      <p class='hint'>💡 Did you know? The first cocktail party was held in 1917.</p>
    </div>
    <div class="questions-container">
      <%= f.input :cocktail_category, as: :radio_buttons, collection: @cocktail_category, input_html: { class: 'remove-button'}, wrapper_html: { class: 'questions-cards' }, label: 'What kind of cocktail would you like?', label_html: { class: 'selectable-card' }, input_html: { class: 'remove-button'} %>
      <p class='hint'>💡 Did you know? There are many cocktail types and categories (gladly, we made it simple for you).</p>
    </div>
    <%= f.submit %>
<% end %>
