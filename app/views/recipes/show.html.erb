<div class="container">
  <h2>Recipe was created!</h2>
  <hr>
  <div class="questions-container">
      <div class="row">
        <div class="col-12 col-md-6">
          <p class="instructions">Your cocktail choice was:</p>
          <h2 class="recipe-title"><%= @recipe.name %></h2>
          <div class="card card-recipe-show" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.4)), url(<%=@recipe.image_url%>); background-size: cover; background-position:center;" >
          </div>
          <hr>
          <p class="ingredient-list">Ingredients:</p>
          <div class="font-size-ingredient"><%= @recipe.ingredient %></div>
          <hr>
          <p class="instructions">How to make it:</p>
          <div class="font-size-instruction"><%= @recipe.instruction %></div>
        </div>
        <hr>
        <p class="ingredient-list">Ingredients:</p>
        <div class="font-size-ingredient"><%= @recipe.ingredient.join(" • ") %></div>
        <hr>
        <p class="instructions">How to make it:</p>
        <div class="font-size-instruction"><%= @recipe.instruction %></div>
      </div>
    <div class="btn-recipe-show">
      <%= link_to "Mix again!", new_preference_path, class: "btn btn-ghost-recipe" %>
    </div>
  </div>
</div>
